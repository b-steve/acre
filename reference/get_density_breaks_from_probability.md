# Calculate density breaks for given probabilities

Calculates appropriate breaks for given density from provided
probability bins.

## Usage

``` r
get_density_breaks_from_probability(dens, probability_breaks)
```

## Arguments

- dens:

  a numeric vector of densities.

- probability_breaks:

  the probabilities at which to calculate density breaks.
